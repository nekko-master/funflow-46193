<h1>プラン詳細</h1>

<p>
  <strong>日付：</strong>
  <%= @plan.date %>
</p>

<p>
  <strong>パーク：</strong>
  <%= @plan.park.name %>
</p>

<p>
  <strong>作成者：</strong>
  <%= @plan.user.nickname %>
</p>

<%= link_to "このプランで簡単診断する", new_diagnosis_path(plan_id: @plan.id) %>

<%= link_to "一覧に戻る", plans_path %>

<%= link_to "プランを削除",
            plan_path(@plan),
            data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" } %>

<h2>行動ステップ</h2>

<% if @plan.plan_steps.any? %>
  <ul>
    <% @plan.plan_steps.order(:step_number).each do |step| %>
      <li>
        <%= step.step_number %>.
        <%= step.action_type.name %> /
        <%= step.target.name %>
        <% if step.time.present? %>
          （<%= step.time.strftime("%H:%M") %>）
        <% end %>
        <% if step.note.present? %>
          （<%= step.note %>）
        <% end %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>まだ行動が登録されていません</p>
<% end %>

<%= link_to "手動で行動を追加", new_plan_plan_step_path(@plan) %>
